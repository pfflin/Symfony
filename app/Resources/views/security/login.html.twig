{% extends 'base.html.twig' %}

{% block main %}
    <form name="authenticate" action="{{ path('security_login') }}" method="post">
        <label>
            <input name="_username" type="text">
        </label>
        <label>
            <input name="_password" type="text">
        </label>
        <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
        <input type="submit" value="Login">
    </form>
    <form name="authenticate" action="{{ path('security_login') }}" method="post">
        <div class="form-group" >
            <label>Email</label>
            <input name="_username" type="email" class="form-control" aria-describedby="emailHelp" placeholder="Enter email">
            <small id="emailHelp" class="form-text text-muted">We'll never share your email with anyone else.</small>
        </div>
        <div class="form-group">
            <label for="exampleInputPassword1">Password</label>
            <input name="_password" type="password" class="form-control" id="exampleInputPassword1" placeholder="Password">
        </div>
        <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
        <button type="submit" value="Login" class="btn btn-primary">Submit</button>
    </form>
{% endblock %}